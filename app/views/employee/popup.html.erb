<%= render "remote_modal", title: "Platforms" do %>
        <div class="row">
            <% @integrations&.each do |integration| %>
                <div class="col-md-12 col-sm-12 d-flex justify-content-between">
                    <div class="d-flex p-2">
                    <%= image_tag integration&.logo ,class: "rounded-circle", style: "height: 28px;"%>
                    <div class="d-flex flex-column" style="padding-left: 10px;">
                        <b><%= integration&.name %></b>
                    </div> 
                    </div>
                    <div>
                        <a href="#" class="btn btn-primary" ><%= image_tag "chevron-down.svg" %> Add</a>
                    </div>
                </div>
                <hr>
            <% end  %>

            <div class="col-md-2 col-sm-2 form-group">
                <label for="start">Start</label>
            </div>
            <div class="col-md-4 col-sm-4 form-group">
                <input type="date" name="start_date" id="" class="form-control">
            </div>
            <div class="col-md-2 col-sm-2 form-group">
                <label for="start">Start</label>
            </div>
            <div class="col-md-4 col-sm-4 form-group">
                <input type="date" name="start_date" id="" class="form-control">
                <%# <%= f.date_field :end_date, class: "form-control", placeholder: "Select date" %> 
            </div>
            <div class="col-md-12 col-sm-12 mt-2 mb-2" align="center">
                <button class="btn btn-primary" style="width: 34%;">Submit</button>
            </div>
        </div>
<% end %>